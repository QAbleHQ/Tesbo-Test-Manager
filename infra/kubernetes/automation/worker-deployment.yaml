apiVersion: apps/v1
kind: Deployment
metadata:
  name: automation-worker
  namespace: bettercases-automation
spec:
  replicas: 0
  selector:
    matchLabels:
      app: automation-worker
  template:
    metadata:
      labels:
        app: automation-worker
    spec:
      containers:
        - name: automation-worker
          # Tesbo-Test-Runner-Agents BullMQ worker (same image as queue API).
          image: registry.digitalocean.com/REPLACE_ME/tesbo-test-runner-agents:latest
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "7411"
            - name: AUTOMATION_SERVICE_ROLE
              value: "worker"
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: automation-secrets
                  key: redis-url
            - name: AUTOMATION_QUEUE_PREFIX
              value: "bull"
            - name: AUTOMATION_QUEUE_NAME
              value: "automation-execution-jobs"
            - name: AUTOMATION_QUEUE_ENABLED
              value: "true"
            - name: AUTOMATION_QUEUE_CONCURRENCY
              value: "2"
            - name: BACKEND_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: automation-secrets
                  key: backend-base-url
            - name: AUTOMATION_QUEUE_SHARED_TOKEN
              valueFrom:
                secretKeyRef:
                  name: automation-secrets
                  key: queue-shared-token
            - name: AGENT_SHARED_TOKEN
              valueFrom:
                secretKeyRef:
                  name: automation-secrets
                  key: agent-shared-token
          readinessProbe:
            httpGet:
              path: /health
              port: 7411
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 7411
            initialDelaySeconds: 20
            periodSeconds: 20
