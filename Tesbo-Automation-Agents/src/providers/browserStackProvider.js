export async function runWithBrowserStackProvider() {
  // When implemented: always end/stop remote sessions in `finally` so metered billing stops.
  throw new Error("BrowserStack provider is not enabled for this environment.");
}
